import actions from './actions'
import mutations from './mutations'
import getters from './getters'
import state from './state'

/**
 * We recommend to have different files for store, actions, getters, etc.
 * if the file is getting too extended. This will make files more readable.
 */
export default {
  actions,
  mutations,
  getters,
  state,
}
